version = "1.0.0"
description = "Fixes moderation actions"

aliucord {
    changelog.set(
        """
        # 1.0.0
        * Initial plugin release.
        """.trimIndent()
    )
    author("Adham", 1001222848716738570L, hyperlink = false)
    deploy.set(true)
}
